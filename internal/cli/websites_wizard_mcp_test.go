// Package cli websites wizard MCP step handler tests.
//
// The per-step handler tests are consolidated into pkg/internal/mcp/wizard_test.go
// which tests the handlers through the session's AdvanceSession API.
package cli
