{
  "private": true,
  "type": "module",
  "devDependencies": {
    "@playwright/test": "latest",
    "@types/node": "latest",
    "sunpeak": "latest",
    "typescript": "latest"
  },
  "scripts": {
    "test": "sunpeak test",
    "test:http-bearer": "playwright test -c playwright.http.config.ts",
    "test:oauth": "playwright test -c playwright.oauth.config.ts"
  }
}
